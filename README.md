## Requirements

To run this script, you need:

* **Python 3.8+**
* **Requests Library**: For handling HTTP POST requests to the Netbeacon API.
* **Netbeacon API Key**: A valid API key provided by Netbeacon to submit reports.

### Dependencies Installation

You can install the required Python packages using `pip`:

```bash
pip install -r requirements.txt
